# Search for alternate names on SSL certs

## Usage
```sh
go run alt-names.go -f <filename> -t <threads>
```
### Filename with hosts
- Must have the format `<host>:<port>`
- One per line

## TODO
- [ ] Save to a file
- [ ] Colored output